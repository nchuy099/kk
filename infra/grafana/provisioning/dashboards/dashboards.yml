apiVersion: 1

providers:
  - name: event-hub
    orgId: 1
    folder: Event Hub
    type: file
    disableDeletion: false
    updateIntervalSeconds: 10
    allowUiUpdates: true
    options:
      path: /var/lib/grafana/dashboards

